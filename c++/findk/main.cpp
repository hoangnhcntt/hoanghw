#include <bits/stdc++.h>
using namespace std;
long long n,a[1000000000];
int main()
{
    freopen("findk.inp","r",stdin);
    freopen("findk.out","w",stdout);
    cin>>n;
    k=(-1+sqrt(1+8.0*n))/2;
    cout<<k;
    return 0;
}
